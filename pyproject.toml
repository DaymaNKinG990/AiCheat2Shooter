[project]
name = "aicheat2shooter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flet>=0.28.3",
    "numpy>=2.3.2",
    "plotly>=6.2.0"
]

[tool.uv.workspace]
members = [
    "task-orchectrator-mcp",
]
